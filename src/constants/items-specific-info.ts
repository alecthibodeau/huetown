/* Constants */
import items from './items';

const latestLunarCalendarId: string = items.lunarCalendar2025.id;
const preorderId: string | undefined = undefined;

const itemsSpecificInfo = {
  latestLunarCalendarId,
  preorderId
};

export default itemsSpecificInfo;
